import { ExerciseUnitPipe } from './exercise-unit.pipe';

describe('ExerciseUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
